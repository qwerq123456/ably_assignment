interface NextButtonProps {
    buttonText: string;
    onClick: () => void;
}

export const NextButton = ({ buttonText, onClick }: NextButtonProps) => {
    return (
        <div>
            nextbutton
        </div>
    );
};
